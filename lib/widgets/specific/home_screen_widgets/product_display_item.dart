import 'package:flutter/material.dart';

class ProductDisplayItem extends StatelessWidget {
  const ProductDisplayItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(
        children: [

        ],
      ),
    );
  }
}